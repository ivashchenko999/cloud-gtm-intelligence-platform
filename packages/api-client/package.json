{
  "name": "@cloud-gtm/api-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "generate": "orval --config ./orval.config.ts",
    "clean": "rm -rf coverage"
  },
  "dependencies": {
    "@cloud-gtm/contracts": "workspace:*",
    "@tanstack/react-query": "^5.62.11"
  },
  "peerDependencies": {
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.18",
    "orval": "^7.3.0",
    "react": "^18.3.1"
  }
}
