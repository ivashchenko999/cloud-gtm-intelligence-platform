# Local Vite runs only the React app. Point it at the deployed AWS API unless
# you explicitly add/run a local API server.
VITE_API_BASE_URL=https://4njro4e2y6.execute-api.ca-central-1.amazonaws.com/api
VITE_DEFAULT_LOCALE=en-CA
